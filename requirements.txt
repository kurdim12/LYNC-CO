# LIV — Phase 0 dependencies.
#
# Pinned to the Pipecat version bot.py + smoke_test.py were VERIFIED against.
# DOCUMENTATION.md §5/§20: Pipecat's API drifts between releases — when you bump
# this pin, re-run `python smoke_test.py` before trusting bot.py.
#
# Extras:
#   deepgram,openai,elevenlabs,simli  -> the four pipeline stages
#   silero                            -> VAD (turn-taking / barge-in)
#   webrtc                            -> local SmallWebRTC transport
#   runner                            -> dev server + prebuilt test client (localhost:7860)
#   anthropic,google                  -> LLM A/B (Claude / Gemini) for the spike (§6.3, §16)
pipecat-ai[deepgram,openai,elevenlabs,simli,silero,webrtc,runner,anthropic,google]==1.3.0
python-dotenv>=1.0
loguru>=0.7
